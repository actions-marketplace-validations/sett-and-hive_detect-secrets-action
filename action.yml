# action.yml
name: "Easy IBM detect-secrets"
description: "Use IBM's detect-secrets package to scan your repository for exposed secrets"
runs:
  using: "docker"
  image: "Dockerfile"
branding:
  icon: "lock"
  color: "green"
