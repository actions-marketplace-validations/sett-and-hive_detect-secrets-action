# action.yml
name: "Detect Secrets"
description: "Use detect-secrets to scan your repository for exposed secrets"
runs:
  using: "docker"
  image: "Dockerfile"
branding:
  icon: "lock"
  color: "gray-dark"
